
// Not start just try to understand what to do
#include <stdarg.h>
#include <unistd.h>
#include <stdio.h>

int ft_printf(const char *, ...)
{
    int tlen = 0;
    int *plen;

    plen = &tlen;
    
    return (tlen);
}

int main(void);
{
    printf("printf = ");
    //ft_printf("ft_printf =");   
    return (0);
}